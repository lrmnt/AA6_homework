// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/lrmnt/AA6_homework/billing/ent/billingcycle"
	"github.com/lrmnt/AA6_homework/billing/ent/operations"
	"github.com/lrmnt/AA6_homework/billing/ent/predicate"
	"github.com/lrmnt/AA6_homework/billing/ent/task"
	"github.com/lrmnt/AA6_homework/billing/ent/user"
)

// OperationsQuery is the builder for querying Operations entities.
type OperationsQuery struct {
	config
	ctx              *QueryContext
	order            []operations.OrderOption
	inters           []Interceptor
	predicates       []predicate.Operations
	withUser         *UserQuery
	withBillingCycle *BillingCycleQuery
	withTask         *TaskQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OperationsQuery builder.
func (oq *OperationsQuery) Where(ps ...predicate.Operations) *OperationsQuery {
	oq.predicates = append(oq.predicates, ps...)
	return oq
}

// Limit the number of records to be returned by this query.
func (oq *OperationsQuery) Limit(limit int) *OperationsQuery {
	oq.ctx.Limit = &limit
	return oq
}

// Offset to start from.
func (oq *OperationsQuery) Offset(offset int) *OperationsQuery {
	oq.ctx.Offset = &offset
	return oq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (oq *OperationsQuery) Unique(unique bool) *OperationsQuery {
	oq.ctx.Unique = &unique
	return oq
}

// Order specifies how the records should be ordered.
func (oq *OperationsQuery) Order(o ...operations.OrderOption) *OperationsQuery {
	oq.order = append(oq.order, o...)
	return oq
}

// QueryUser chains the current query on the "user" edge.
func (oq *OperationsQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: oq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := oq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := oq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(operations.Table, operations.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, operations.UserTable, operations.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(oq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBillingCycle chains the current query on the "billing_cycle" edge.
func (oq *OperationsQuery) QueryBillingCycle() *BillingCycleQuery {
	query := (&BillingCycleClient{config: oq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := oq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := oq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(operations.Table, operations.FieldID, selector),
			sqlgraph.To(billingcycle.Table, billingcycle.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, operations.BillingCycleTable, operations.BillingCycleColumn),
		)
		fromU = sqlgraph.SetNeighbors(oq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTask chains the current query on the "task" edge.
func (oq *OperationsQuery) QueryTask() *TaskQuery {
	query := (&TaskClient{config: oq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := oq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := oq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(operations.Table, operations.FieldID, selector),
			sqlgraph.To(task.Table, task.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, operations.TaskTable, operations.TaskColumn),
		)
		fromU = sqlgraph.SetNeighbors(oq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Operations entity from the query.
// Returns a *NotFoundError when no Operations was found.
func (oq *OperationsQuery) First(ctx context.Context) (*Operations, error) {
	nodes, err := oq.Limit(1).All(setContextOp(ctx, oq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{operations.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (oq *OperationsQuery) FirstX(ctx context.Context) *Operations {
	node, err := oq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Operations ID from the query.
// Returns a *NotFoundError when no Operations ID was found.
func (oq *OperationsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = oq.Limit(1).IDs(setContextOp(ctx, oq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{operations.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (oq *OperationsQuery) FirstIDX(ctx context.Context) int {
	id, err := oq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Operations entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Operations entity is found.
// Returns a *NotFoundError when no Operations entities are found.
func (oq *OperationsQuery) Only(ctx context.Context) (*Operations, error) {
	nodes, err := oq.Limit(2).All(setContextOp(ctx, oq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{operations.Label}
	default:
		return nil, &NotSingularError{operations.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (oq *OperationsQuery) OnlyX(ctx context.Context) *Operations {
	node, err := oq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Operations ID in the query.
// Returns a *NotSingularError when more than one Operations ID is found.
// Returns a *NotFoundError when no entities are found.
func (oq *OperationsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = oq.Limit(2).IDs(setContextOp(ctx, oq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{operations.Label}
	default:
		err = &NotSingularError{operations.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (oq *OperationsQuery) OnlyIDX(ctx context.Context) int {
	id, err := oq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OperationsSlice.
func (oq *OperationsQuery) All(ctx context.Context) ([]*Operations, error) {
	ctx = setContextOp(ctx, oq.ctx, "All")
	if err := oq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Operations, *OperationsQuery]()
	return withInterceptors[[]*Operations](ctx, oq, qr, oq.inters)
}

// AllX is like All, but panics if an error occurs.
func (oq *OperationsQuery) AllX(ctx context.Context) []*Operations {
	nodes, err := oq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Operations IDs.
func (oq *OperationsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if oq.ctx.Unique == nil && oq.path != nil {
		oq.Unique(true)
	}
	ctx = setContextOp(ctx, oq.ctx, "IDs")
	if err = oq.Select(operations.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (oq *OperationsQuery) IDsX(ctx context.Context) []int {
	ids, err := oq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (oq *OperationsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, oq.ctx, "Count")
	if err := oq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, oq, querierCount[*OperationsQuery](), oq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (oq *OperationsQuery) CountX(ctx context.Context) int {
	count, err := oq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (oq *OperationsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, oq.ctx, "Exist")
	switch _, err := oq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (oq *OperationsQuery) ExistX(ctx context.Context) bool {
	exist, err := oq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OperationsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (oq *OperationsQuery) Clone() *OperationsQuery {
	if oq == nil {
		return nil
	}
	return &OperationsQuery{
		config:           oq.config,
		ctx:              oq.ctx.Clone(),
		order:            append([]operations.OrderOption{}, oq.order...),
		inters:           append([]Interceptor{}, oq.inters...),
		predicates:       append([]predicate.Operations{}, oq.predicates...),
		withUser:         oq.withUser.Clone(),
		withBillingCycle: oq.withBillingCycle.Clone(),
		withTask:         oq.withTask.Clone(),
		// clone intermediate query.
		sql:  oq.sql.Clone(),
		path: oq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (oq *OperationsQuery) WithUser(opts ...func(*UserQuery)) *OperationsQuery {
	query := (&UserClient{config: oq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	oq.withUser = query
	return oq
}

// WithBillingCycle tells the query-builder to eager-load the nodes that are connected to
// the "billing_cycle" edge. The optional arguments are used to configure the query builder of the edge.
func (oq *OperationsQuery) WithBillingCycle(opts ...func(*BillingCycleQuery)) *OperationsQuery {
	query := (&BillingCycleClient{config: oq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	oq.withBillingCycle = query
	return oq
}

// WithTask tells the query-builder to eager-load the nodes that are connected to
// the "task" edge. The optional arguments are used to configure the query builder of the edge.
func (oq *OperationsQuery) WithTask(opts ...func(*TaskQuery)) *OperationsQuery {
	query := (&TaskClient{config: oq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	oq.withTask = query
	return oq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UUID uuid.UUID `json:"uuid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Operations.Query().
//		GroupBy(operations.FieldUUID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (oq *OperationsQuery) GroupBy(field string, fields ...string) *OperationsGroupBy {
	oq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OperationsGroupBy{build: oq}
	grbuild.flds = &oq.ctx.Fields
	grbuild.label = operations.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UUID uuid.UUID `json:"uuid,omitempty"`
//	}
//
//	client.Operations.Query().
//		Select(operations.FieldUUID).
//		Scan(ctx, &v)
func (oq *OperationsQuery) Select(fields ...string) *OperationsSelect {
	oq.ctx.Fields = append(oq.ctx.Fields, fields...)
	sbuild := &OperationsSelect{OperationsQuery: oq}
	sbuild.label = operations.Label
	sbuild.flds, sbuild.scan = &oq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OperationsSelect configured with the given aggregations.
func (oq *OperationsQuery) Aggregate(fns ...AggregateFunc) *OperationsSelect {
	return oq.Select().Aggregate(fns...)
}

func (oq *OperationsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range oq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, oq); err != nil {
				return err
			}
		}
	}
	for _, f := range oq.ctx.Fields {
		if !operations.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if oq.path != nil {
		prev, err := oq.path(ctx)
		if err != nil {
			return err
		}
		oq.sql = prev
	}
	return nil
}

func (oq *OperationsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Operations, error) {
	var (
		nodes       = []*Operations{}
		withFKs     = oq.withFKs
		_spec       = oq.querySpec()
		loadedTypes = [3]bool{
			oq.withUser != nil,
			oq.withBillingCycle != nil,
			oq.withTask != nil,
		}
	)
	if oq.withUser != nil || oq.withBillingCycle != nil || oq.withTask != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, operations.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Operations).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Operations{config: oq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, oq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := oq.withUser; query != nil {
		if err := oq.loadUser(ctx, query, nodes, nil,
			func(n *Operations, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := oq.withBillingCycle; query != nil {
		if err := oq.loadBillingCycle(ctx, query, nodes, nil,
			func(n *Operations, e *BillingCycle) { n.Edges.BillingCycle = e }); err != nil {
			return nil, err
		}
	}
	if query := oq.withTask; query != nil {
		if err := oq.loadTask(ctx, query, nodes, nil,
			func(n *Operations, e *Task) { n.Edges.Task = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (oq *OperationsQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*Operations, init func(*Operations), assign func(*Operations, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Operations)
	for i := range nodes {
		if nodes[i].operations_user == nil {
			continue
		}
		fk := *nodes[i].operations_user
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "operations_user" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (oq *OperationsQuery) loadBillingCycle(ctx context.Context, query *BillingCycleQuery, nodes []*Operations, init func(*Operations), assign func(*Operations, *BillingCycle)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Operations)
	for i := range nodes {
		if nodes[i].operations_billing_cycle == nil {
			continue
		}
		fk := *nodes[i].operations_billing_cycle
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(billingcycle.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "operations_billing_cycle" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (oq *OperationsQuery) loadTask(ctx context.Context, query *TaskQuery, nodes []*Operations, init func(*Operations), assign func(*Operations, *Task)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Operations)
	for i := range nodes {
		if nodes[i].operations_task == nil {
			continue
		}
		fk := *nodes[i].operations_task
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(task.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "operations_task" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (oq *OperationsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := oq.querySpec()
	_spec.Node.Columns = oq.ctx.Fields
	if len(oq.ctx.Fields) > 0 {
		_spec.Unique = oq.ctx.Unique != nil && *oq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, oq.driver, _spec)
}

func (oq *OperationsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(operations.Table, operations.Columns, sqlgraph.NewFieldSpec(operations.FieldID, field.TypeInt))
	_spec.From = oq.sql
	if unique := oq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if oq.path != nil {
		_spec.Unique = true
	}
	if fields := oq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, operations.FieldID)
		for i := range fields {
			if fields[i] != operations.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := oq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := oq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := oq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := oq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (oq *OperationsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(oq.driver.Dialect())
	t1 := builder.Table(operations.Table)
	columns := oq.ctx.Fields
	if len(columns) == 0 {
		columns = operations.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if oq.sql != nil {
		selector = oq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if oq.ctx.Unique != nil && *oq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range oq.predicates {
		p(selector)
	}
	for _, p := range oq.order {
		p(selector)
	}
	if offset := oq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := oq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OperationsGroupBy is the group-by builder for Operations entities.
type OperationsGroupBy struct {
	selector
	build *OperationsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ogb *OperationsGroupBy) Aggregate(fns ...AggregateFunc) *OperationsGroupBy {
	ogb.fns = append(ogb.fns, fns...)
	return ogb
}

// Scan applies the selector query and scans the result into the given value.
func (ogb *OperationsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ogb.build.ctx, "GroupBy")
	if err := ogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OperationsQuery, *OperationsGroupBy](ctx, ogb.build, ogb, ogb.build.inters, v)
}

func (ogb *OperationsGroupBy) sqlScan(ctx context.Context, root *OperationsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ogb.fns))
	for _, fn := range ogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ogb.flds)+len(ogb.fns))
		for _, f := range *ogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OperationsSelect is the builder for selecting fields of Operations entities.
type OperationsSelect struct {
	*OperationsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (os *OperationsSelect) Aggregate(fns ...AggregateFunc) *OperationsSelect {
	os.fns = append(os.fns, fns...)
	return os
}

// Scan applies the selector query and scans the result into the given value.
func (os *OperationsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, os.ctx, "Select")
	if err := os.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OperationsQuery, *OperationsSelect](ctx, os.OperationsQuery, os, os.inters, v)
}

func (os *OperationsSelect) sqlScan(ctx context.Context, root *OperationsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(os.fns))
	for _, fn := range os.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*os.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := os.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
